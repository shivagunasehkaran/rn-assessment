/**
 * Common generic types used across the application
 */

/**
 * Request/operation status states
 */
export type RequestStatus = "idle" | "loading" | "succeeded" | "failed";

/**
 * Generic ID type
 */
export type ID = string | number;
